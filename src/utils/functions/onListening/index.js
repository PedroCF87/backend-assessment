exports.onListening = props => {
  console.log(`|----------------------------------------------|`)
  console.log(`|> Listening to ${props.host} server on port ${props.port} <|`)
  // console.log(`|----------------------------------------------|`)
  // console.log(`|> API Docs: ----------------------------------|`)
  // console.log(`|> http://${props.host}:${props.port}/docs_v1.html ---------|`)
  console.log(`|----------------------------------------------|`)
}